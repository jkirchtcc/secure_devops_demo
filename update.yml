---
- name: Update and upgrade packages on localhost
  hosts: local
  become: yes
  vars_files:
    - vault.yml
  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes

    - name: Upgrade all packages
      apt:
        upgrade: dist


